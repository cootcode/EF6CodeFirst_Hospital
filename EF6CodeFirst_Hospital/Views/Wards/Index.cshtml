@model IEnumerable<EF6CodeFirst_Hospital.Models.Ward>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Hospital.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BedsNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsFull)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Hospital.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BedsNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsFull)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WardId }) |
            @Html.ActionLink("Details", "Details", new { id=item.WardId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WardId })
        </td>
    </tr>
}

</table>
